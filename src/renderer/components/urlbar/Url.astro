---
// TODO It should be an input with preprend
---

<div class="border border-white/50 w-full h-screen rounded-lg flex gap-2 pl-2 items-center">
  <span class="loading loading-spinner loading-sm hidden" id="tab-loading"></span>
  <input class="outline-0 w-full h-full rounded-lg" readonly />
</div>

<script>
  const loadingIndicator = document.getElementById('tab-loading')!;
  // loadingIndicator.classList.remove('hidden');
  // setTimeout(() => {
  //   loadingIndicator.classList.add('hidden');
  // }, 1000);

  abUrlBar.onRefresh((event, data) => {
    console.log(data);
  });
</script>
