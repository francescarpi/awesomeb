---
title: New window
---

import FeatureAccess from '../../../components/FeatureAccess.astro'

<FeatureAccess entity="new window" macOS="Cmd+n" command="newWindow"/>

Opening a new window lets you have separate contexts within the same browser. For example, you can have one window for work and another for personal use, each with its own active desktop and tab list.
