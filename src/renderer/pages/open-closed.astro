---
import BlackBgWithTitle from '#/layouts/BlackBgWithTitle.astro';
import ListWithSearch from '#/components/common/ListWithSearch.astro';
---

<BlackBgWithTitle title="Open Closed Tabs">
  <ListWithSearch placeholder="Filter and select a tab..." id="list" />
</BlackBgWithTitle>

<script>
  import { getSearchParams, listWithSearchManager } from '#/scripts';

  const { winId } = getSearchParams();

  listWithSearchManager('list', {
    entity: 'closedTabs',
    onAccept: (item) => {
      abCommands.perform(winId, 'open-closed', { url: item.id });
    },
    onEscape: () => {
      abModal.close(winId);
    },
  });
</script>
