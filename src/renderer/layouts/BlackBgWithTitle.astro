---
import Base from './Base.astro';

interface Props {
  title?: string;
}
const { title } = Astro.props;
---

<Base>
  <div class="bg-black text-white p-4 fixed inset-0">
    <h1 class="text-lg mb-2">{title}</h1>
    <slot />
  </div>
</Base>
