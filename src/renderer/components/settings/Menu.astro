---
const PAGES = [
  { name: 'General', section: '' },
  { name: 'Profiles', section: 'profiles' },
  { name: 'Themes', section: 'themes' },
  { name: 'Permissions', section: 'permissions' },
];
---

<div role="tablist" class="tabs tabs-box mt-4">
  {
    PAGES.map((page) => (
      <a role="tab" class="tab [--tab-bg:black]" href={`?${page.section}`}>
        {page.name}
      </a>
    ))
  }
</div>

<script>
  function updateActiveTab() {
    const section = window.location.search.substring(1);
    const tabs = document.querySelectorAll('.tab');

    tabs.forEach((tab) => {
      tab.classList.remove('tab-active');

      const href = tab.getAttribute('href');
      if (href === `?${section}`) {
        tab.classList.add('tab-active');
      }
    });
  }

  document.addEventListener('DOMContentLoaded', () => updateActiveTab());
  document.addEventListener('hashchange', () => updateActiveTab());
</script>
