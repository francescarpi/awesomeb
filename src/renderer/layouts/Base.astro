---
import '../assets/app.css';

interface Props {
  backgroundColor?: string;
  borderRadius?: string;
  backgroundImage?: string;
}

const { backgroundColor, borderRadius, backgroundImage } = Astro.props;
---

<!doctype html>
<html lang="en" data-theme="corporate">
  <head>
    <meta charset="UTF-8" />
    <meta
      http-equiv="Content-Security-Policy"
      content="default-src 'self' 'unsafe-inline'; script-src 'self' 'unsafe-inline'; img-src 'self' awesomeb: data: https:; connect-src 'self' ws:; worker-src blob:;"
    />
    <meta name="viewport" content="initial-scale=1, width=device-width" />
  </head>
  <body><div class="rootContainer"><slot /></div></body>
</html>

<style
  define:vars={{
    backgroundColor: backgroundColor || 'transparent',
    borderRadius: borderRadius || '0px',
    backgroundImage: backgroundImage || 'none',
  }}
>
  .rootContainer {
    position: fixed;
    inset: 0;
    background-color: var(--backgroundColor);
    color: black;
    border-radius: var(--borderRadius);
    background-image: var(--backgroundImage);
  }
</style>
