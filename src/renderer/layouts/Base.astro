---
interface Props {
  backgroundColor?: string;
  borderRadius?: string;
}

const { backgroundColor, borderRadius } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      http-equiv="Content-Security-Policy"
      content="default-src 'self' 'unsafe-inline'; script-src 'self' 'unsafe-inline'; img-src 'self' awesomeb: data: https:; connect-src 'self' ws:; worker-src blob:;"
    />
    <meta name="viewport" content="initial-scale=1, width=device-width" />
  </head>
  <body><div class="rootContainer"><slot /></div></body>
</html>

<style
  define:vars={{ backgroundColor: backgroundColor || 'white', borderRadius: borderRadius || '0px' }}
>
  .rootContainer {
    position: fixed;
    inset: 0;
    background-color: var(--backgroundColor);
    app-region: drag;
    color: black;
    border-radius: var(--borderRadius);
  }
</style>
