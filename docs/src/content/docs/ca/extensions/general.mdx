---
title: Extensions
---

**AwesomeB** permet, encara que d'una manera una mica rudimentària, extensions de *Chrome Store*.

:::danger
Compte! Encara estan *work in progress* i són bastant inestables. No estan implementades totes les APIs ni hi ha una integració amb el *Chrome Store*.
:::

### Comportament de les APIs disponibles

Les API d'extensions següents estaran disponibles amb aquests comportaments:

* `tabs.query({ active: true })` retornarà només les pestanyes actives.
* `tabs.query` retornarà el resultat i podrà invocar opcionalment una callback amb les pestanyes.
* `cookies.getAll` retornarà les galetes i podrà invocar opcionalment una callback amb aquestes.
* `tabs.create` obrirà sense canvis les URL absolutes `http://` i `https://`.
* Les URL relatives que rebi `tabs.create` es resoldran respecte de l'origen de l'extensió.
* Els paràmetres de consulta d'AwesomeB, `partitionId` i `winId`, s'afegiran de manera segura abans de qualsevol fragment de la URL.
* `tabs.create` rebutjarà els esquemes d'URL no compatibles.

:::note
Sé que existeixen algunes llibreries per incorporar extensions a *Electron*, així com descarregar-les del *Chrome Store*, però hi ha algunes limitacions de llicències i he preferit fer la meva pròpia implementació.
:::
